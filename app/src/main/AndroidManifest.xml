<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions to pick images from gallery -->

    <!-- Internet access — required for Cloudinary upload and Firestore -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Image picker — Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Image picker — Android 12 and below -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:theme="@style/Theme.LoFo.Splash"
        android:name=".LoFoApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_icon"
        android:supportsRtl="true"
        tools:targetApi="31">

<!--        AuthActivity is the launcher-->
        <activity
            android:name=".auth.AuthActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"/>
    </application>

</manifest>

<!--WHY: AuthActivity checks Firebase on every cold start.-->
<!--If user is logged in → instantly goes to MainActivity.-->
<!--If not → shows Login screen.-->
<!--This gives you "Stay Logged In" for free via Firebase's built-in session persistence.-->