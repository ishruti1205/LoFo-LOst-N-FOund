<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- When item is pressed (clicked) -->
    <item android:state_pressed="true" android:color="@color/color_accent"/>

    <!-- Default -->
    <item android:color="@color/white"/>

</selector>